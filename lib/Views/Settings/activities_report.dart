import 'package:flutter/material.dart';

class ActivitiesReports extends StatelessWidget {
  const ActivitiesReports({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Activities report"),
      ),
    );
  }
}
